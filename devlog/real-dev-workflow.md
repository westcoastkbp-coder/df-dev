# Real Dev Workflow

- Ran operator workflow with commands: `status`, `test`, `open`.
- Verified system status through `DF-SYSTEM-STATUS`.
- Verified tests through `DF-RUN-TESTS`.
- Verified preview path through `DF-BUILD-WEBSITE`, which ensured `runtime/out/landing/index.html` exists.
