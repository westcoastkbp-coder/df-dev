# Runtime Boundary

- Normalized runtime paths to `runtime/out`, `runtime/state`, and `runtime/logs`.
- Moved task memory to `runtime/state/task_memory.json`.
- Removed drive-qualified runtime path strings from controlled execution outputs.
