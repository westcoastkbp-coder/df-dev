# Runtime Smoke Check

- Added `scripts/smoke_check.py` to verify the required runtime directories and `runtime/state/task_memory.json`.
- Script prints a structured JSON result with overall status and per-check status.
